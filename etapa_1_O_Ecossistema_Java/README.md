# 📂 Etapa 01: O Ecossistema Java (JDK, JRE, JVM)

Começando pela base do java, entender como a ferramente funciona antes mesmo de apertar o botão. 

## 🛠️ Desafios Executados

### 1. Criando o primeiro "Olá, Mundo!" (`OlaMundo.java`)
Criar um simples "Olá, Mundo!"
* **Conceito Chave:** Gerar o Bytecode utilizando o `javac` e o `java` para chamar a JVM e executar o programa